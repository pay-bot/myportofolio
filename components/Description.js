export default function Description({ children }) {
  return (
    <p
      data-aos="fade-up"
      className="capitalize mb-10 text-gray-600 dark:text-gray-200  text-base "
    >
      {children}
    </p>
  );
}
